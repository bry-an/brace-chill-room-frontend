const baseURL = process.env.NODE_ENV === 'development' ? 'http://localhost:3001' : '';

export default {
  spotifyLogin() {
  // some function that does all of that login-y business
    console.log('spotify login');
  },
};
